# openengine
Vendor-neutral gRPC (proto3) API for the runtime contract between inference orchestrators and model engines like SGLang, TensorRT-LLM, and vLLM. The .proto is the single source of truth for build-time gRPC stub generation.
